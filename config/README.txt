source .venv/bin/activate (activate your virtual environment)

uv pip install -r requirments.txt (installs requirements recursively) 

run installer
uv run python pico_sync.py

run manager 
uv run python pico_sync_manager.py